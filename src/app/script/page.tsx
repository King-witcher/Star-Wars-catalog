'use client'

export default function Page() {
  // const fields: Record<string, string[]> = {}
  // function addIfNotExists(key: string, value: string) {
  //   if (fields[key] && !fields[key].includes(value)) fields[key].push(value)
  //   else if (!fields[key]) fields[key] = [value]
  // }
  // for (const person of people) {
  //   for (const [key, value] of Object.entries(person)) {
  //     if (typeof value === 'string') {
  //       addIfNotExists(key, value)
  //     } else {
  //       for (const single_value of value) {
  //         addIfNotExists(key, single_value)
  //       }
  //     }
  //   }
  // }
  // console.log(fields)
  // return <code>{JSON.stringify(people)}</code>
}
